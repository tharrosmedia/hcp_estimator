{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter frontend install --frozen-lockfile && pnpm --filter frontend build"
  },
  "deploy": {
    "startCommand": "pnpm --filter frontend start"
  }
}

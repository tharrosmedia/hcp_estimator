{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "pnpm install --frozen-lockfile",
        "pnpm build"
      ]
    }
  },
  "deploy": {
    "startCommand": "pnpm start"
  }
}

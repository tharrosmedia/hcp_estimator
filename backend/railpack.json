{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "...",
        "pnpm --filter hcp-estimator-backend install --frozen-lockfile",
        "pnpm --filter hcp-estimator-backend build"
      ]
    }
  },
  "deploy": {
    "startCommand": "pnpm --filter hcp-estimator-backend start"
  }
}

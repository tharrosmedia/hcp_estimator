{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter hcp-estimator-backend install --frozen-lockfile && pnpm --filter hcp-estimator-backend build"
  },
  "deploy": {
    "startCommand": "pnpm --filter hcp-estimator-backend start"
  }
}
